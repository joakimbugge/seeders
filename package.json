{
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev:watch": "concurrently -n build,typecheck -c cyan,magenta \"pnpm --parallel -r run dev\" \"pnpm run typecheck:watch\"",
    "typecheck:watch": "tsc -b tsconfig.typecheck.json --watch --preserveWatchOutput",
    "build": "pnpm -r run build",
    "lint:fix": "pnpm -r run lint:fix",
    "fmt": "pnpm -r run fmt",
    "test": "pnpm -r run test",
    "typecheck": "tsc -b tsconfig.typecheck.json",
    "test:cov": "vitest run --coverage",
    "docs:typedoc": "(cd docs && typedoc --options typedoc.seeder.json && typedoc --options typedoc.typeorm-seeder.json && typedoc --options typedoc.nest-typeorm-seeder.json && typedoc --options typedoc.mikroorm-seeder.json && typedoc --options typedoc.nest-mikroorm-seeder.json)",
    "docs:dev": "pnpm run docs:typedoc && vitepress dev docs",
    "docs:build": "pnpm run docs:typedoc && vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "@faker-js/faker": "^10.5.0",
    "@swc/core": "^1.15.47",
    "@types/node": "^26.2.0",
    "@vitest/coverage-v8": "^4.1.10",
    "better-sqlite3": "^12.11.1",
    "concurrently": "^10.0.4",
    "lefthook": "^2.1.10",
    "oxfmt": "^0.63.0",
    "oxlint": "^1.78.0",
    "tsdown": "^0.22.14",
    "tsx": "^4.23.12",
    "typedoc": "^0.28.20",
    "typedoc-plugin-markdown": "^4.12.0",
    "typescript": "^6.0.3",
    "unplugin-swc": "^1.5.11",
    "vitepress": "^1.6.4",
    "vitest": "^4.1.10"
  }
}
