pre-commit:
  parallel: false
  commands:
    lint:
      run: npm run lint:fix --workspaces --if-present
      stage_fixed: true
    format:
      run: npm run fmt --workspaces --if-present
      stage_fixed: true
